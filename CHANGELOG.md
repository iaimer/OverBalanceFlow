# 更新日志

## v1.0.2 — 2026-08-14

### 修复
- ZIP 备份文件改为「偷闲半日-备份-日期-时间.zip」，避免同日多次导出覆盖
- 正式包从清洁构建缓存生成，确保首次启动直接读取线上 Supabase 账本

---

## v1.0.1 — 2026-08-14

### 变更
- 正式名称更新为「偷闲半日」，替换 Android 启动器图标并提供自适应图标资源
- 记加班与记调休 Dock 图标统一为单色时钟图形，分别使用公文包与 FREE 牌子区分业务

### 修复
- 启动时先显示 Flutter 界面，线上读取配置 12 秒超时并自动回退缓存，避免网络阻塞造成黑屏
- 照片缓存改为后台任务，不再阻塞账本首屏；全部 Supabase 读写与迁移请求均有明确超时

---

## v1.0.0 — 2026-08-13（Flutter Android）

### 新增
- Supabase 恢复为权威账本，SQLite 改为离线只读缓存；ZIP 支持校验后合并恢复到云端
- 日期类型按官方年度安排自动区分工作日、法定节假日和普通周末，设置页移除手工修正入口
- 采用深青、金黄、珊瑚橙和奶油色的新视觉体系，加班与调休使用独立页面主题
- 记加班页增加实时预计时长，拍照与相册按钮改为同一行等宽布局
- 统计页重排余额、累计加班和累计调休的信息层级
- 将 PWA 重构为 Flutter Android 本地应用，旧实现完整归档至 `legacy_web/`
- 四入口 Material 界面、照片、FIFO 核销和完整历史
- Supabase 云端权威账本、SQLite 离线缓存和全字段数据指纹
- ZIP 哈希备份、UUID 合并恢复、恢复前数据库与照片完整快照
- 2026 节假日日历、年度 JSON 导入和单日类型修正

### 安全
- 调试版与正式版包名和数据目录隔离
- 核销、删除返还使用待审阅部署的 PostgreSQL 原子 RPC
- 迁移和恢复均先准备文件，再提交数据库；失败清理未提交附件
- 正式迁移强制匹配外部只读记录/UUID/原始照片路径与 SHA-256 基线
- 禁用正式包名的 debug/profile 变体，缺少 release keystore 时阻断正式构建
- 生产基线导出 58 条历史记录，源 Supabase 未执行任何写入

---

## v1.2.0 — 2026-07-08

### 新增
- 加班记录支持打卡照片：移动端可直接拍照，也可从相册选择
- 照片在前端压缩（最长边 1600px、JPEG 0.82）后上传 Supabase Storage（`ot-record-photos` 桶）
- 离线时照片暂存 IndexedDB，恢复网络后随记录一起同步
- 记录列表展示打卡照片卡片，点击可查看大图
- 删除记录时同步清理照片对象，失败则入队待网络恢复后清理
- 新增 `supabase_photo_storage.sql`（建桶 + 匿名上传/删除策略）

---
## v1.1.1 — 2026-05-31

### 修复
- 离线新增记录同步后正确改写临时 ID，避免后续核销或删除失效
- 服务端拒绝同步时保留队列项，不再静默丢失更新或删除操作
- 离线临时 ID 增加随机后缀，避免同毫秒连续写入发生碰撞
- 调休备注使用 `textContent` 渲染，修复存储型 XSS 风险
- 核销前检查预览余额是否过期，并处理写入失败
- 调休存根 `total_hours` 与 `duration` 保持负值一致
- Service Worker 支持 GitHub Pages 子路径部署，并缓存 Supabase SDK
- 更新横幅移除内联 `onclick`

### 文档
- 补充离线临时 ID 映射和依赖重放约束

---

## v1.1.0 — 2026-05-28

### 新增
- 记加班页面底部显示最近 2 条加班记录
- 自动识别周末和法定节假日，跳过 17:00/18:00 规则
- 新增 `PRODUCT.md`（产品定义文档）、`DESIGN.md`（设计规范文档）

### 变更
- 加班配色从蓝色调整为土黄色（#c49a2a），符合纸本风格
- 统计页面从深色卡片改为白底纸卡样式
- 移除 Tailwind CDN，使用自定义 CSS design tokens

### 修复
- 周末/节假日加班被错误拦截（13:10-17:34 报"时间无效"）
- 调休补班日（周末上班）正确识别为工作日

### 文档
- `README.md` 更新技术栈与项目结构说明
- `AGENTS.md` 新增 `renderRecentRecords`、设计文档引用、核销预览流程

---

## v1.0.1 — 2026-05-27

### 修复
- 加班起始时间未截断至 17:00，导致多计时长（如 9:00-20:00 计入 11h 而非 3h）

---

## v1.0.0 — 2026-05-27

### 安全修复
- **XSS 漏洞**：用户 memo 文本使用 textContent 替代 innerHTML
- **删除双倍返还漏洞**：先删数据库再返还余额，防止重试重复加回

### 功能性修复
- **离线队列数据丢失**：`initApp()` 启动时调用 `syncPendingOps()`
- **错误静默吞没**：`syncPendingOps` 的 catch 改为 `console.warn`
- **离线横幅文案错误**：修正误导用户的信息
- **时区解析异常**：`parseDate` 增加守卫判断
- **加载状态缺失**：表单提交和删除操作显示 loading 反馈
- **localStorage 版本迁移**：`cache_version` 校验
- **SW 更新检测**：新版本刷新横幅
- **内联 onclick**：删除按钮改为 `data-delete-btn` + `addEventListener`

### 其他
- 新增 `AGENTS.md`（AI 开发规范）、`README.md`（人类使用文档）
- `fetchRecords` 统一使用 `getCached`/`setCached` 路径

---

## v0.4.2 — 2026-05-27

### 修复
- 调休核销不受 18:00 限制

---

## v0.4.1 — 2026-05-27

### 修复
- 时长计算改为向下取整到 0.5h（18:31 不再错误记为 2h）
- 新增规则：加班结束时间必须 ≥ 18:00

---

## v0.4.0 — 2026-05-27

### 变更
- 时长取整改为向上取整到 0.5h

---

## v0.3.1 — 2026-05-27

### 新增
- PNG 应用图标、favicon、Apple Touch Icon
- manifest.webmanifest 支持 maskable

---

## v0.3.0 — 2026-05-27

### 新增
- Service Worker 支持
- 离线缓存策略
- PWA 可安装

---

## v0.2.0 — 2026-05-27

### 新增
- 加班/调休记录 CRUD
- Supabase 数据同步
- FIFO 核销逻辑
- 统计分析仪表盘
- 加班时长计算（含午休扣减、最小 0.5h）
- 四种状态管理：待核销 / 部分核销 / 已结清 / 已调休

---

## v0.1.0 — 2026-05-27

### 新增
- 项目初始化
