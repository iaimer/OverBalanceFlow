# 偷闲半日 — Product

**register**: product

## 一句话

个人加班调休记账工具。Supabase 保存权威账本，手机可离线查看、独立备份并恢复。

## 用户与目标

一个人使用，无认证、团队或审批。用户需要快速记加班、按 FIFO 核销调休，并确信历史记录不会因断网、调试、升级或换手机丢失。

## 核心功能

- 记录加班日期、时间、备注与打卡照片
- 按工作日、节假日和午休规则计算时长
- 预览并执行 FIFO 调休核销
- 统计余额并查看完整历史和核销明细
- Supabase 作为权威账本，SQLite 仅提供离线只读缓存
- 导出、校验并合并恢复 ZIP 备份
- 自动判断工作日、官方法定节假日和普通周末休息日

## 非目标

- 不做团队和审批
- ZIP 完整校验后可按 UUID 合并恢复到 Supabase，不自动删除云端既有记录
- 不发通知，不生成企业报表

## 数据主权

Supabase 是日常权威数据源，Android 私有目录保存最后一次完整 SQLite/照片缓存。备份和恢复提供机器可验证的 SHA-256；用户可以通过普通 ZIP 携带完整记录与照片。
